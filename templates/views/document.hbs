<div class="container">
	<div class="row">
		<div class="col-sm-10 col-sm-offset-1 col-md-8 col-md-offset-2">
			<article>
				<p class="documents_title"><a href="/documents">&larr; 返回上一级</a></p>
				<hr>
				<header>
					<h1>{{data.document.title}}</h1>
					<h5>
						{{{documentCategoryList data.document.categories prefix="Documented in "}}}
						{{#if author.name.first}}by {{author.name.first}}{{/if}}
					</h5>
				</header>
				<div class="post">
					{{#if data.document.image}}<img src="{{{cloudinaryUrl data.document.image width=750 height=450 crop='fit' }}}" class="img-responsive">{{/if}}
					{{{data.document.content.extended}}}
				</div>
			</article>
		</div>
	</div>
</div>
